<h1 class="mb-5">Events</h1>

<%= link_to 'New event', new_event_path, class: 'btn btn-success' %>

<div id="events", class="my-4">
  <% @events.each do |event| %>
    <%= render event %>
    <p>
      <%= link_to "Show this event", event, class: 'btn btn-info my-4' %>
    </p>
  <% end %>
</div>
