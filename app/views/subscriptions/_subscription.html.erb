<div class="subscription d-flex align-items-center border-bottom border-bottom-1 py-1">
  <% if subscription.user.present? %>
    <%= render partial: 'users/user', object: subscription.user %>
  <% else %>
    <em><%= subscription.user_name %></em>
  <% end %>
  <% if policy(subscription).destroy? %>
    <%= button_to '❌', event_subscription_path(subscription.event, subscription), method: :delete,
                  form: { data: { turbo_confirm: t('.destroy_confirm') } },
                  title: t('views.buttons.delete'), class: 'btn btn-outline delete' %>
  <% end %>
</div>
