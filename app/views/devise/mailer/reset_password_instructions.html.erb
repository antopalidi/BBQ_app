<p>Hello <%= @resource.email %>!</p>

<p><%= t('devise.mailer.reset_password_instructions.description') %></p>

<p><%= link_to t('devise.mailer.reset_password_instructions.change_password'), edit_password_url(@resource, :reset_password_token => @token) %></p>

<p><%= t('devise.mailer.reset_password_instructions.text_ignore') %></p>
<p><%= t('devise.mailer.reset_password_instructions.text_link') %></p>
